---
layout: single
title:  "열거형의 학습"
categories: Programming
tag: [Programming, TIL, CSharp]
toc: true
sidebar:
    nav: "counts"
typora-root-url: ../
---



# 열거형의 학습
- 학습 목표
  - 열거형의 정의와 사용 목적을 이해하고, 열거형의 선언 방법과 사용법을 숙지하는 것


## A. 문서 이력
- 최초 작성일 : 2025-12-14
- 최종 수정일 : 2025-12-14

- 작성 툴 : Typora, Visual Studio Code

- 정보 출처 : 경일게임IT아카데미 프로그래밍 4기 - 본과정 6일차 강의 내용, [MSDN C# 열거형 형식 문서](https://learn.microsoft.com/ko-kr/dotnet/csharp/language-reference/builtin-types/enum) 등



## B. 열거형의 정의와 기초 이해
- 열거형 형식은 기본적인 정수 숫자 형식을 사용하여 정의된 값 형식
- 열거형을 사용하면 숫자에 벌명을 붙여 알아보기 쉽게 만들어줄 수 있어, 소스 코드의 가독성을 높일 수 있음



## C. 열거형의 선언 방법 및 사용 방법
- `enum`을 사용하고 `열거형 멤버의 이름`을 지정하여 열거형을 사용
- `enum`은 기본적으로 0으로 시작, 그러나 번호를 바꿀 수 있음
- 열거형 사용 예시
  - 다양한 종족과 국가가 존재하는 세계관에서 각 종족과 세력을 열거형으로 구분
  ```csharp
  // 열거형 선언
  enum Factions // 같은 종족인데 다른 문화권이 추가될 수 있기에, 명칭은 {종족} + {정치체 / 지리 등 문화}로 지음
  {
    HumanKingdom = 1, // 1 = 왕국 인가
    HumanHighland = 2,  // 2 = 고지 인간
    ElfEmpire,  // 3 = 제국 엘프
    ElfForest,  // 4 = 숲 엘프
    OrcSteppe,  // 5 = 스텝 오크
    OrcDessert,  // 6 = 사막 오크
  }

  // 열거형 사용

  static void Main()
  {
      // enum 변수 선언 및 값 할당 가능
      Factions yourFaction;
      yourFaction = Factions.HumanKingdom;

      // 형변환 기능을 이용하여 숫자로도 enum 값 할당 가능
      Factions NeutralFaction;
      NeutralFaction = (Factions)3;

      // 콘솔 입력으로 열거형 값 할당 가능
      Factions enemyFaction;
      Enum.TryParse(Console.ReadLine(), out enemyFaction);    // 직접 입력 시, 1, 2 등 숫자 입력 가능, HumanKingdom, ElfForest 등 미리 선언한 열거형으로도 가능

      // 열거형을 사용하기 위한 switch 문
      switch (yourFaction)
      {
          case Factions.HumanKingdom:
              Console.WriteLine("You are Human Kingdom.");
              break;
          case Factions.HumanHighland:
              Console.WriteLine("You are Human Highland.");
              break;
          case Factions.ElfEmpire:
              Console.WriteLine("You are Elf Empire.");
              break;
          case Factions.ElfForest:
              Console.WriteLine("You are Elf Forest.");
              break;
          case Factions.OrcSteppe:
              Console.WriteLine("You are Orc Steppe.");
              break;
          case Factions.OrcDessert:
              Console.WriteLine("You are Orc Dessert.");
              break;
          default:
              Console.WriteLine("Unknown faction."); // 발생할 일 없어야 하는 기본 값
              break;
      }

      // switch 패턴 일치 식
      string factiongString = yourFaction switch
      {
          Factions.HumanKingdom => "You are Human Kingdom.",
          Factions.HumanHighland => "You are Human Highland.",
          Factions.ElfEmpire => "You are Elf Empire.",
          Factions.ElfForest => "You are Elf Forest.",
          Factions.OrcSteppe => "You are Orc Steppe.",
          Factions.OrcDessert => "You are Orc Dessert.",
          _ => "Unknown faction." // 발생할 일 없어야 하는 기본 값
      };

      Console.WriteLine(factiongString);  // switch 패턴 일치 식의 값을 콘솔이 출력
      Console.WriteLine((int)NeutralFaction);  // 형변환 기능을 사용한 값이 int 형식으로 출력
      Console.WriteLine(enemyFaction);  // 콘솔 입력받은 값이 출력    
  }
  ```

열거형 활용 및 인자값으로 넘겨보기
Console.ForegroundColor, Console.BackgroundColor 도 `enum`으로 정의됨


추가적인 팁
Enum.IsDefined(typeof(<mark>열거형 명칭</mark>), <mark>대상 숫자</mark>); 기능 사용 시 지정된 값의 상수가 해당 열거형에 있는지 표시하는 것을 True / False로 반환함